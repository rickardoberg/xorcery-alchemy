jars:
  sourcejars:
    - jar: excel
      name: excel
      enabled: "{{ jars.enabled }}"
      context:
        sourceUrl: null
  transmutejars:
    - jar: excel
      name: excel
      enabled: "{{ jars.enabled }}"
      context:
        resultUrl: null
