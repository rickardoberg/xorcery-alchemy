jars:
  sourcejars:
    - jar: excel
      name: excel
      enabled: "{{ jars.enabled }}"
      context:
        sourceUrl: null
  resultjars:
    - jar: excel
      name: excel
      enabled: "{{ jars.enabled }}"
      context:
        resultUrl: null
