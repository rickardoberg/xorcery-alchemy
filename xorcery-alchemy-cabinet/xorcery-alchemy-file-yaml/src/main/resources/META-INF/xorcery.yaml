jars:
  sourcejars:
    - jar: yaml
      name: yaml
      enabled: "{{ jars.enabled }}"
      context:
        sourceUrl: null
  transmutejars:
    - jar: yaml
      name: yaml
      enabled: "{{ jars.enabled }}"
      context:
        resultUrl: null
