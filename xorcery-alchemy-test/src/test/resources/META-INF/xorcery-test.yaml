$schema: xorcery-alchemy-test-override-schema.json

defaults:
  enabled: false

jars:
  enabled: true

crucible:
  enabled: true
  closeWhenDone: false

opentelemetry:
  enabled: true

log4j2:
  Configuration:
    thresholdFilter:
      level: debug
    Loggers:
      logger:
        - name: dev.xorcery.junit
          level: debug
