jars:
  sourcejars:
    - jar: yaml
      name: yaml
      enabled: "{{ jars.enabled }}"
      context:
        sourceUrl: null
  resultjars:
    - jar: yaml
      name: yaml
      enabled: "{{ jars.enabled }}"
      context:
        resultUrl: null
