jars:
  sourcejars:
    - jar: projection
      name: projection
      enabled: "{{ jars.enabled }}"
      context:
        projectionId: null
  transmutejars:
    - jar: projection
      name: projection
      enabled: "{{ jars.enabled }}"
      context:
        projectionId: null
